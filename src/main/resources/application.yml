spring:
  application:
    name: getir-books-service
  datasource:
   url : jdbc:mysql://localhost:3306/reading_is_good?verifyServerCertificate=false&useSSL=false
   username : root
   password : root
   driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database : MYSQL
    show-sql : true
    hibernate:
     ddl-auto : update
    database-platform : org.hibernate.dialect.MySQL5InnoDBDialect
server:
  port: 8080